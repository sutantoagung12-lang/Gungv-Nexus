# Bridge Security

The public bridge is intentionally sanitized.

It must never expose:
- private repository names or topology
- memory or knowledge records
- credentials, API keys, tokens, cookies or secrets
- internal file contents
- private URLs

Only safe system metadata and safety policy may be published.

The bridge is read-only. It does not execute actions.
